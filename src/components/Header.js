import { PureComponent } from "react"

export const Header = () => {
    return (
        <header className="header">
            <div className="title">
                <h1>Spotlessify!</h1>
            </div>
        </header>
    )
}